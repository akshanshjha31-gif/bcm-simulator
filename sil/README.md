# Software-in-the-Loop (Phase 5)

Host build that links the real service/logic layer against simulated drivers,
letting the entire BCM behaviour run on a PC and be driven by the WPF tool over
a virtual COM port. Includes scripted scenarios and a pass/fail report generator
(SRS-TST-002).
