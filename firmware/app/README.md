# Application Layer (Phase 4)

Composition root and FreeRTOS task bodies. Wires concrete drivers into the
service managers (dependency injection), runs POST, and hosts the task set
described in `docs/architecture/ArchitectureDocument.md` §4.
