/**
 * @file    test_main.cpp
 * @brief   Catch2 entry point. Kept in its own translation unit so the
 *          framework is compiled once rather than per test file.
 */
#define CATCH_CONFIG_MAIN
#include "catch2/catch.hpp"
