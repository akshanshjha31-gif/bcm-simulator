# Host Unit Tests (Phase 1+)

GoogleTest suites compiled natively (no hardware). Targets: UART frame parser,
checksum, all four FSMs, lighting/door safety logic, config manager, logger,
fault manager. Coverage goal ≥ 80 % lines on logic modules (SRS-TST-001).
